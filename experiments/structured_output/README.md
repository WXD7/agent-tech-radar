# 结构化输出与校验失败恢复实验

状态：计划中。

固定同一模型、同一嵌套 schema、同一错误样本和相同最大重试次数，对 Pydantic AI、OpenAI Agents SDK 与 LangGraph 记录：

- 首次成功率；
- 平均重试次数；
- 最终失败时的错误可读性；
- trace 是否包含模型输出、校验错误和重试原因；
- 完成时间与模型调用量。

在接入模型凭证以前，此实验不会运行，也不会伪造结果。

