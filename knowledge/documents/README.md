# Research documents

Research documents are the primary long-form knowledge unit in Agent Tech Radar.
YAML files hold document metadata and graph relationships; Markdown files hold the
readable body. Private conversation-derived documents live in `private/` and are
excluded from Git by default.
