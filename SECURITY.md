# 安全与隐私说明

Agent Tech Radar 当前是个人本地 Demo，不是经过安全审计的多用户服务。

## 默认运行边界

- 只建议绑定 `127.0.0.1`。
- 不要将当前 Web 端口直接暴露到公网。
- 当前的 CSRF 防护不等于登录、授权或租户隔离。
- 导入会话的任务 ID、节点级原文锚点、私有笔记和私有专题图谱默认被 `.gitignore` 排除。

## 敏感信息

请不要提交：

- API Key、OAuth Token、Cookie 或其他会话凭据。
- 私人 Codex 任务 ID、未脱敏的会话摘要或本地用户路径。
- `knowledge/conversations/anchors/`、`knowledge/nodes/private/` 和 `knowledge/maps/private/` 中的本机学习数据。
- `.env`、`.radar/`、虚拟环境、测试缓存或本地数据库。
- 包含私有源码、内部文档或他人个人信息的调研证据。

GitHub API 的可选 Token 应通过 `GITHUB_TOKEN` 环境变量传入。

## 外部内容与 Agent 执行

官方文档、GitHub 仓库、论文、Issue、README 和社区列表都应视为外部输入，而不是可信操作指令。

如果后续接入会执行命令的 Codex、Prime Agent 或其他长期 Agent：

- 对不受信仓库使用临时 clone、干净工作树或外部沙箱。
- 限制可写目录、网络、凭据、运行时间和费用预算。
- 将 worker、kernel 或子进程隔离视为生命周期隔离，不将其宣称为安全沙箱。
- 对 Agent 生成的代码、Skill、Memory 和长期经验做差异审查并保留回滚点。

## 公开发布前检查

1. 确认仓库可见性与其中数据的敏感级别匹配。
2. 检查 Git 将要提交的完整文件列表，而不只看当前工作目录。
3. 搜索凭据模式、个人路径、邮箱、会话深链接和私有网址。
4. 如果凭据曾经进入 Git 历史，不要只删除当前文件；应立即撤销/轮换凭据并清理历史。

发现安全问题时，在项目还是个人种子阶段的情况下，请优先私下通知维护者，不要在公开 Issue 中粘贴敏感细节。
